<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ title }}</title>
        <meta charset="utf-8">
        <link href='http://fonts.googleapis.com/css?family=Lato:100' rel='stylesheet' type='text/css'>
        <link rel="stylesheet" type="text/css" href="css/compareweather.css">
    </head>
    <body>
        <div class="weather">
            <div class="locations group">
                {{{ location0Html }}}
                {{{ location1Html }}}
            </div>
            <p class="comparison">{{{ comparisonSentenceHtml }}}</p>
        </div>
    </body>
</html>
